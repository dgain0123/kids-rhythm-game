# 關卡音樂：每個大關卡的音樂全都不一樣

> 這是「章節音樂風格」這條規則的**唯一正解檔**。要改規則先改這裡，
> 程式（`tools/music_style.py`、`tools/track_music.py`）與測試
> （`tests/test_music_style.py`）以本檔為準。

## ★ 規矩（2026-08-02 使用者定案）

**每一個大關卡（章節）的背景音樂都要跟其他大關卡明顯不同**——
**調性、樂器音色、編曲/和聲** 都要換，讓小朋友一聽就知道「換章節了」。
同一個大關卡底下的小關卡則共用同一套風格（只有速度/長度不同），聽起來是同一個世界。

> **節拍器音色是例外，可以跟別章一樣**（2026-08-03 使用者裁示「提示音跟節拍器都用
> 第一大關原本的就好」）。我曾把第二大關卡的節拍器調成 1500+3400Hz 的尖亮點擊，
> 量到 4kHz 以上能量是第一大關卡的兩倍，使用者直接聽出「雜」→ 現在兩章共用
> **木魚 1100Hz**（`ChimePadStyle.click` 同款）。

**光換音色不夠（2026-08-03 使用者打槍過一輪）**：同一套骨架（正弦疊加音色＋長 pad＋
貝斯＋同樣編曲）只改音色參數，聽起來還是同一個世界。要換就連
**合成方式／編曲結構／空間感／音階語言**一起換 —— 或者乾脆換條路線用現成曲目。

## 三條路線

| | ① 程式合成 | ② 現成曲目 | ③ **自製 MIDI ＋ SoundFont render**（2026-08-03 會議決議，現行首選） |
|---|---|---|---|
| 實作 | `tools/music_style.py`（`Style` 子類別，`kind="synth"`） | `tools/track_music.py`＋`kind="track"` 的風格 | 自己寫 MIDI → `fluidsynth -R 0 -C 0` 離線 render → `kind="track"` ＋ `aligned_render=True` |
| 適合 | 要完全掌控每個音、極慢速關卡 | 要「真樂器/真編曲/兒歌」的氛圍，最省事 | **要「專業音質＋完全乾聲＋零授權風險」**——現成錄音一定有房間殘響，土砲正弦合成音色又廉價 |
| 素材 | 無 | 放 `sounds/music/source/`，登記在 [來源與授權](../sounds/music/source/來源與授權.md) |
| 授權 | 不用管 | **只收 CC0／公共領域／CC BY**；**NC、ND、SA 一律不准**（我們會變速＝改作，ND 不相容；SA 會傳染專案）。CC BY 的素材，出處**必須顯示在遊戲畫面上**（`index.html` 頁尾），測試會擋 |
| 變速 | 直接照關卡拍點合成 | 自動變速對齊（見下） |

## 不變的部分（跟章節、路線都無關）

1. **軟起音樂器／音量壓低**：避免喇叭聲被麥克風的打鼓偵測誤判成鼓聲（見 [打鼓偵測](打鼓偵測.md)）。
2. **拍點音最亮**：小朋友要打的那一下，一定是當下最明顯的聲音（現成曲目就靠節拍器擔任）。
3. **節拍器只在拍點上響**（每個拍點一聲木魚）。
   ⚠️ **預備拍底下不要疊節拍器**——第一大關卡本來就是「預備拍＝純人聲」，
   我在第二大關卡自作主張加了墊底節拍器，使用者聽出來「雜」（2026-08-03 改回）。
4. **預備拍**：4 拍英文人聲 one/two/three/four（間隔規矩見 [關卡系統](關卡系統.md)）。
   **人聲固定用本地檔**，不要每次重打 TTS（見下方「數拍人聲」）。
5. 音樂**烤成單一 m4a**（含節拍器與人聲），遊戲只播一個檔。
6. **音量：只調音量、完全不壓縮**（2026-08-03 使用者裁示「全拿掉壓縮」）。
   - `Mixer.finish()` 預設走 **clean 模式**（`CLEAN_MASTER=True`）：整軌等比例放大到
     峰值 `CLEAN_PEAK=0.97`，**不壓縮、不限幅**。
   - **「不壓縮還能再大聲嗎」的物理上限**：峰值頂到 1.0 就削波，所以從 0.89 推到 0.97
     只換得 **+0.9 dB**，就沒了。要再大只能壓縮（把峰值壓下來才推得上去）——
     兩者不可兼得，已選擇乾淨。想更大聲就轉喇叭音量。
   - 編碼後**自動驗證解碼峰值**（`_decoded_peak`）：AAC 過衝量因素材而異，
     破表就自動降一點重編（最多 4 次）。
   - ⚠️ **量峰值一定要用原生取樣率＋float**：降取樣(8k/22k)的濾波器自己會過衝，
     會假破表（實測 level14 原生 0.977、降到 8k 量成 1.097，害我差點誤判）。
   - 為什麼：壓縮會①吃掉動態（實測把 14.7dB 的 crest factor 壓成 6.7dB）
     ②把預備拍人聲的編碼雜訊抬起來（使用者一聽就抓到）。
     代價是整體比壓縮版小約 **6 dB**——**所有關卡一起小，所以彼此仍然一致**。
   - 音效（歡呼/失敗）也照同一標準：`tools/remaster_sfx.py`（只調音量），
     目標＝音樂的響段響度 -12.9 dBFS。
   - 改標準時：音樂用產生器**重跑**（壓縮是破壞性的，只調音量救不回動態；
     人聲已固定用 `sounds/voice/*.wav` 本地檔，重跑不會變聲），音效用 `remaster_sfx.py`。
   - 由 `tests/test_music_style.py` 釘住：**各關音量差 ≤3dB、動態 ≥10dB（防壓縮復辟）、峰值 ≤1.0**。

## 目前的風格登記表（`tools/music_style.py` 的 `STYLES`）

| 大關卡 | 章節名 | 路線 | 風格 | 調性 | 樂器 | 節拍器 |
|---|---|---|---|---|---|---|
| 1 | 第一行第一小節（level9~18） | 合成 | 鐘琴音樂盒＋暖 pad | C 大調（C G Am F） | 鐘琴＋雙振盪暖 pad＋正弦貝斯 | 木魚 1100Hz |
| 2 | 第一行第二小節（level20~） | **自製 MIDI＋SoundFont** | 布拉姆斯搖籃曲・木琴馬林巴 | C 大調 3/4 華爾滋（**一小節＝拍點的整數倍**：速度10＝一小節1拍點=90BPM、速度20＝1拍點=180BPM、**速度30＝3拍點=90BPM**——照算的 270BPM 使用者說太趕） | 馬林巴取樣音源（GeneralUser GS）：旋律＋分解和弦＋低音 | 木魚 1100Hz（與第一大關卡共用） |

備用風格（`CANDIDATES`，開新章節可直接用或當範本）：
合成款＝音樂盒(F大調/三角鐵)、馬林巴(G大調/沙鈴)、撥弦烏克麗麗(D大調/木塊)
—— **注意這三款骨架相同、氛圍偏近**，用前先確認跟既有章節真的不一樣；
現成曲目款＝旋轉木馬風琴(CC0)、Fluffing a Duck(Kevin MacLeod, CC BY)。

> **完整做法（數拍人聲怎麼換、路線③ 怎麼 render、現成曲目怎麼變速對齊、素材去哪找、
> 對齊與音量怎麼驗）→ [關卡音樂・做法詳解](關卡音樂_做法詳解.md)**

## 實際步驟 ①：新增一個大關卡

1. **選風格**：做 2~4 個候選各約 15 秒（用正式管線做，聽到的＝之後會出貨的聲音），
   `afplay` 放給使用者聽、讓他挑。走現成曲目就先在
   [Openverse API](https://api.openverse.org/v1/audio/?q=…&license=cc0) 撈 CC0 素材。
2. **登記**：`tools/music_style.py`
   - 合成路線：新增 `Style` 子類別（實作 `hit/chord/bass/fill/click`、填
     `name/key/instruments/metronome/prog`）。
   - 現成曲目：新增 `kind="track"` 的類別，填 `source/license/credit/source_url` 與 `click`，
     素材放 `sounds/music/source/`，並在 [來源與授權](../sounds/music/source/來源與授權.md) 加一列。
   - 在 `STYLES` 加一筆 `章節編號: 新風格()`，**要跟其他章節明顯不同**。
3. **在 `js/main.js` 的 `GROUPS` 加這個大關卡**（章節編號＝在 GROUPS 裡的順序，1 起算）。
4. **跑守門**：`python3 tests/guard_test.py` → 綠才算數。
5. 章節名與關卡表更新到 [關卡系統](關卡系統.md)，本檔登記表也補一行。

## 實際步驟 ②：在某個大關卡底下新增一關（跟拍關卡）

1. 寫 `tools/make_levelN_music.py`，**不要自己內建音色**，照章節拿風格：
   ```python
   from music_style import style_for_chapter
   from track_music import render          # 現成曲目路線
   CHAPTER = 2
   STYLE = style_for_chapter(CHAPTER)
   render(STYLE, hit_sec=該關拍點間隔, n_hits=要打幾下,
          out_path=".../sounds/music/levelN.m4a", lead_hits=4, pre=1.0)
   ```
   （合成路線改用 `music_style.Mixer` ＋ `STYLE.chord/bass/hit/click`。）
2. 產音樂 → 轉譜（`tools/mid2json.py`，`--music/--lead-in/--pre-roll/--metronome`）。
3. **驗音樂與譜面對齊**（一定要做，見下）。
4. 加進 `js/main.js` 該章節的 `levels`、`sw.js` 的預抓清單（chart + m4a）、寫 `關卡/第N關.md`。
5. `python3 tests/guard_test.py` 全綠。

## 守門機制（規則不會被忘記）

改任何 `.py` 會由全域 PostToolUse hook 自動跑 `tests/guard_test.py`，紅就擋下編輯。

`tests/test_music_style.py`（音樂/授權/音量）：
① `js/main.js` 的每個大關卡都要有 `STYLES` 條目（漏加就紅，訊息直接告訴你怎麼補）
② 任兩章的 **調性／樂器／和聲進行** 不可以一樣（節拍器音色是例外、允許共用）
③ 沒定義風格的章節呼叫 `style_for_chapter` 一定要爆（不可默默沿用別章音樂）
④ **現成曲目章節**：素材要在、授權在白名單（CC0/公共領域/CC BY，禁 NC/ND/SA）、
   來源登記在 來源與授權.md、CC BY 的出處要真的顯示在 `index.html`
⑤ 每個跟拍關卡 json 指到的音樂檔真的存在
⑥ **音樂要放得比最後一顆音符的容許窗久**（長度 ≥ 最後一下＋容許值＋0.5 秒）——
   音樂放完還有沒打到的音符就判失敗，tail 給太短會冤枉小朋友（新關卡最容易漏這條）
⑦ **素材一小節＝拍點間隔的整數倍**（產生器的 `BAR ÷ HIT` 要整除）——
   不整除小節線就落在拍點之間、整首愈跑愈偏；素材檔也要真的在
⑧ **音量**：各關 RMS 差 ≤3dB、**動態 ≥10dB（防壓縮復辟）**、原生取樣率下峰值 ≤1.0
⑨ **音效**（歡呼/失敗）響段響度要跟音樂差 ≤2.5dB、峰值 ≤1.0
⑩ 所有產生器都要走共用母帶處理，**不准自己做峰值正規化**

`tests/test_ui_rules.py`（畫面/互動，用「原始碼有沒有照規則寫」守門）：
⑪ **譜面換燈點＝拍點本身**，不准出現 `tolSec * 0.6`／`laneEnterLeadSec`／取最近音符
   這三種提前量寫法（三種都被使用者打槍過）
⑫ **燈要一直亮著**（還沒到拍點時退回第一顆未打音符）
⑬ **預備拍＝純人聲**，節拍器不可以墊在底下（兩條路徑都檢查）
⑭ **數拍人聲本地檔**（`sounds/voice/one..four.wav`）必須存在，否則會回頭打 TTS
⑮ **角色超過 8 隻要換行縮小**（`#face.many`，手機一排 4 隻）
⑯ **三連音繪製**：標「3」、時值 1/3、五線譜下移 14px、最小間距 32px

**實測過**：臨時在 `GROUPS` 加沒風格的章節 → 立刻紅；把第2章換成現成曲目 → 抓到「沒有和聲進行」
「缺 來源與授權.md」；這次補釘樁時 → 抓到「現成曲目路徑的預備拍還墊著節拍器」。
